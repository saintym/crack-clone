{
  "_안내": "시나리오별 조정값. 이 파일은 선택이다. 없으면 전역 기본값을 쓴다. 모르는 필드(이 줄처럼)는 무시한다.",
  "_responseChars": "한 응답의 목표 글자 수. 프롬프트에 '약 min~max자를 목표로 한다. (max+500)자를 넘기지 마라'로 들어간다. min > max, 0 이하, 20000 초과는 무시하고 기본값(800~1500)을 쓴다.",
  "responseChars": {
    "min": 800,
    "max": 1500
  }
}
