# Bug Tracker

## 요약

| ID | 제목 | 심각도 | 상태 | 카테고리 |
|----|------|--------|------|----------|
| BUG-001 | 프론트 DTO 필드명 불일치 | 높음 | 수정완료 | 프론트-백 연동 |
| BUG-002 | 채팅 히스토리 파싱 오류 | 높음 | 수정완료 | 프론트-백 연동 |
| BUG-003 | 응답 완료 API body key 불일치 | 중간 | 수정완료 | 프론트-백 연동 |
| BUG-004 | SSE 스트리밍 파싱 오류 | 높음 | 수정완료 | 프론트 |
| BUG-005 | 시나리오 데이터 경로 불일치 | 중간 | 수정완료 | 백엔드 |
| BUG-006 | 시나리오 등록 시 원본 문서 덮어쓰기 | 높음 | 수정완료(T12) | 백엔드 |
| BUG-007 | "수정됨" 표시가 후보별이 아님 | 낮음 | 보류 | 백엔드 |
| BUG-008 | PG에서 첫 기록 이후 기억 기록 실패 | 높음 | 수정완료(T21) | 백엔드 |
| BUG-009 | 읽음 처리가 RUNNING 기록까지 적용 | 중간 | 수정완료(T21) | 백엔드 |
| BUG-010 | 전역 CSS 리셋이 Tailwind 여백 무력화 | 중간 | 수정완료(T21) | 프론트 |

## 패턴 분석

### 공통 원인: 프론트-백엔드 계약 불일치 (BUG-001~003)
프론트엔드 코드를 작성할 때 백엔드 DTO/API 스펙을 직접 확인하지 않고 추정으로 작성하여 발생.

### PG 방언 문제 (BUG-008)
테스트가 H2로만 돌아 PostgreSQL에서만 실패하는 쿼리가 머지됐다. T21에서 JPQL의 "파라미터 IS NULL" 패턴을 금지하는 검사 테스트를 넣었다. 근본 대책(테스트에 실제 PG 도입, Testcontainers 등)은 후속 과제다.

### CSS cascade layer (BUG-010)
Tailwind v4는 유틸리티를 `@layer utilities`에 넣는다. 레이어 밖의 규칙은 명시도와 무관하게 레이어 안 규칙을 이기므로, `index.css`에 직접 쓰는 전역 규칙은 `@layer base`나 `@layer components`에 둔다.

### 수정 계획

#### 즉시 (완료)
- [x] BUG-001: DTO 필드명 수정
- [x] BUG-002: 히스토리 파싱 수정
- [x] BUG-003: body key 수정
- [x] BUG-004: SSE 파싱 로직 수정

#### 단기 (다음 작업)
- [x] BUG-005 근본 수정 (T02): DB 경로 컬럼 제거, `DataPaths`로 `data-path + name + dir_name` 계산, V4 마이그레이션
- [x] BUG-006 수정 (T12): `ScenarioService`가 이미 있는 파일을 덮어쓰지 않음, 회귀 테스트 추가
- [x] BUG-008~010 수정 (T21): PG 파라미터 타입 추론 실패 쿼리 분리, 읽음 처리 대상 한정, 전역 CSS를 cascade layer로 이동
- [ ] 프론트-백엔드 API 타입 자동 동기화 방안 검토 (OpenAPI 스펙 생성 또는 공유 타입 정의)

#### 중기 (안정화)
- [ ] E2E 테스트 추가: 프론트에서 실제 API 호출하여 응답 형식 검증
- [ ] SSE 스트리밍 통합 테스트 추가
- [ ] 데이터 경로 관련 통합 테스트 추가
