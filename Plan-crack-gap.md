# Crack 대화 기능 격차 분석 (Gap Analysis)

> 조사일: 2026-09-22 · 목적: 원작 **크랙(Crack, 뤼튼)** 의 대화 관련 기능과 crack-clone의 현재 구현을 비교해, 앞으로의 목표(로드맵)로 삼는다.
> 프로젝트 성격: **개인용 취미 사이트 + 포트폴리오.** 과금(크래커)·성인인증·크리에이터 정산 같은 상용 서비스 요소는 범위에서 제외한다.

**표기**
- 크랙 쪽: **[공식]** = 공식 공지와 고객센터에서 확인, **[커뮤니티]** = 사용자 후기와 공략, **[추정]** = 역공학이거나 간접 근거
- 우리 쪽 상태: ✅ 완료 · 🟡 부분 구현 · 🔌 백엔드만 있고 UI 없음 · 💤 코드는 있으나 호출되지 않음 · ❌ 없음

---

## 0. 한눈에 보기

| 영역 | 크랙 | crack-clone | 격차 |
|---|---|---|---|
| 기본 채팅 + 스트리밍 | ✅ | ✅ | 작음 |
| 재생성 / 수정 / 삭제 / 분기 | 후보 선택형 재생성, 유저·AI 메시지 수정, 메시지 시점 분기 | 재생성하면 원본 삭제, AI 메시지만 수정, 분기할 때 기억을 잃음 | **중간** |
| 요약 메모리 | 4탭(단기/장기/관계도/목표), 편집 가능, N 뱃지 | 10턴 파일 요약. 기본 설정에서는 **동작하지 않음** | **큼** |
| 유저노트 / 대화 프로필 | 유저노트 500~2000자, 프로필 100개 | protagonist.md 1개(시나리오 단위), must_remember는 UI 없음 | **큼** |
| 시작 설정 / 인트로 / 추천 답변 | 시작 설정 여러 개, 프롤로그, 추천 답변 3개 | 모두 없음 | **큼** |
| 키워드북 / 단축어 | 조건부 설정 주입, `/` 명령 | 없음. 대신 캐릭터 문서를 **전부** 매 턴 주입 | **큼** |
| 스탯 / 엔딩 / 상태창 | 게이지, 레벨별 행동 프롬프트, 엔딩 카드 | 상태 DB(CRUD)만 있고 자동 추출과 UI는 없음 | 큼 |
| 모델 / 출력 길이 / 생각 깊이 | 모드 11종, 최대 출력량, 생각 깊이 4단계 | 프로바이더 2종(CLI/API) 선택만 가능 | 중간 |
| 이미지 | 상황 이미지 배치표, 실시간 삽화 생성 | 없음 | 큼(후순위) |
| 파티챗 | 2~5인 턴제 | 없음 | 범위 밖에 가까움 |

```
피드백:
1. 모델 선택은 어차피 개인용으로 작성한거라 현재 상태처럼 CLI, API 로 놨둬도 돼.
2. 수정 기능은 유저답변과 AI답변 모두를 유저가 수정할 수 있어야해. 그 후 진행할때도 수정된 내용으로 진행되어야 하고. 분기 기능은 후순위로 미뤄도 돼.
3. 요약메모리는 4탭에 나눠 구현하는게 좋아보이긴해. 하지만, 나는 오히려 더 고도화 할 계획이야. 가장 중요한 부분이고 가장 크리티컬한 부분이 기억부분이야.
너무 길면 읽는데 너무 많은 토큰이 소요되어 버려.
그렇기에 다음과 같이 구상중이야.
3-1. 각 사전인물 문서에 발생한 해당 인물간의 사건, 감정변화를 주로 하려 기억을 작성한다. 이는 자잘한 것보다 관계에 영향을 줄 정도의 큰 변화가 아니면 적는걸 피해야한다. 물론 영구적인 소지품이나 기술, 신체변화에 대한 것들 역시 기록해야한다.
3-2. 각 인물간의 사건을 각 인물문서에 기록 후, 주인공(사용자 혹은 유저)의 기억 내용에도 동일하게 변화내용(스탯이나 기술, 관계변화 등)을 적는다.
3-3. 마지막으로 전체 시나리오 담당 문서에 사건의 흐름을 기록해간다. 이는 시나리오가 매우 길어지고 방대해 질시, 전체 시나리오 요약용으로 사용될 것이다.
3-4. 작업의 양이 많고 복잡해지기 때문에, 매 턴마다 실행하는게 아닌 사용자가 특정 명령어로 요청하거나, 특정턴 횟수 (10턴 등)마다 시행한다.
4. 키워드북('/'를 이용한 시나리오 외적 혹은 내적 기능)은 추가해야하는 기능이다.
5. 캐릭터의 상태를 나타내는 UI 작성은 필요하나, 엔딩이나 실시간 급변화하는 정보들까지 매턴 변화를 적용해가며 출력할 필요는 없다.
6. 이미지는 외부 url 을 띄울뿐이다. 물론 프론트에서 출력할떄는 해당 url(webp 파일 등)을 가져와 보여준다.
7. 파티챗은 이후에도 만들 예정은 없다.
8. 추가적으로, 캐릭터 문서 등도 '원본 문서'에 포함되기에 특정한 시나리오로 시작하면 복사하여 매 스토리마다 다르게 저장해야한다. 이것은 A 시나리오로 플레이하다가 완전히 새롭게 A 시나리오를 처음부터 플레이하려고 할떄, 동일한 시나리오의 서로 다른 스토리가 서로의 스토리에 영향을 주면 안된다는 critical 한 원칙을 지키기 위함이다.
9. 그렇게 하나의 스토리를 시작하면, agent 를 병렬로 돌려 '캐릭터 관리자', '시나리오 관리자', '사용자 대응' 등 나눠서 관리해야한다. 캐릭터관리자와 시나리오관리자로부터 정보를 얻고, 해당 정보와 유저의 요청을 바탕으로 스토리를 진행한 후에 이 응답을 캐릭터 관리자와 시나리오 관리자가 특정턴 또는 유저의 요청에 의해 읽어들이고 기록하여 남기는 방식.
```
---

## 1. 응답 조작

| 기능 | 크랙 | crack-clone 현재 | 차이 / 할 일 |
|---|---|---|---|
| **재생성** | 메시지 하단 버튼(Ctrl+Alt+R). "선택되지 않은 메시지"라는 표현으로 보아 **여러 후보 중 하나를 골라 이어가는 구조** [공식+추정]. 리롤이나 삭제 시 스탯 자동 동기화 [공식]. **재생성 지시**(방향 지정) 예고, 2026-09 [공식] | 🟡 마지막 AI 메시지를 **먼저 파일에서 삭제**한 뒤 다시 생성. 후보를 보관하지 않고, 스트림이 실패하면 원본을 잃음 (`ChatService.kt:125-158`) | 후보 스와이프(‹ 1/3 ›) 방식으로 전환하고, 원본은 성공한 뒤에 교체. 재생성 지시 입력란 추가 |
| **수정** | AI·유저 메시지 모두 수정 가능. 2026-01부터 입력창에서 편집. 인트로도 수정 가능 [공식] | 🟡 백엔드는 모든 인덱스를 수정할 수 있지만 UI는 **AI 메시지만** (`ChatPage.tsx:486`) | 유저 메시지 수정 UI 추가. 수정 후 "여기서부터 다시 생성" 옵션 |
| **삭제** | 메시지별 삭제, 복구 불가 [공식]. 해당 메시지만 지우는지, 그 뒤까지 지우는지는 미확인 | 🟡 인덱스 i 이후를 **모두** 잘라냄. turnCount를 chat_latest 기준으로 다시 세서 **요약된 턴이 사라짐** (`ChatService.kt:113-123`) | 턴 번호 기반으로 재설계 (§8 P0) |
| **분기** | 메시지 "더보기 → 분기"로 그 시점까지 복제한 **새 채팅방** 생성 (2026-03-20) [공식] | 🟡 메시지 시점 분기는 됨. 그러나 **chat_latest만 복사**하고 요약·아카이브·캐릭터 복사본·must_remember·DB 상태는 잃음 (`StoryService.kt:79-109`) | 분기 시점까지의 기억 전체 복사. **트리형 분기 뷰**로 차별화 가능 |
| **자동 재생 (이어쓰기)** | 빈 입력창에서 재생 버튼 또는 Enter를 누르면 AI가 이야기를 이어감 (2026-01) [공식] | 🟡 "계속 이어서 작성해주세요."를 **실제 유저 메시지로 파일에 기록** (`ChatService.kt:160-188`) | 가상 지시로 바꿔 히스토리를 오염시키지 않기. UX는 빈 Enter로 |
| 답변 평가 | 좋아요/싫어요 [공식] | ❌ | 선택 사항. 프롬프트 튜닝 로그로 쓸 수 있음 |
| 되돌리기 | 별도 기능 없음. 분기와 삭제로 대체 [추정] | ❌ | 트리 분기로 해결 |

## 2. 기억 / 맥락 (크랙과의 핵심 격차)

| 기능 | 크랙 | crack-clone 현재 | 차이 / 할 일 |
|---|---|---|---|
| **요약 메모리** | 4영역으로 구성 [공식, 2026-02 개편]. **단기 기억**(최근 흐름 타임라인), **장기 기억**(이전 단기 기억이 자동으로 쌓이며 유저가 **최대 100개 추가·수정**), **관계도**(인물과 유저의 관계·태도), **목표**(현재 목표). 갱신되면 N 뱃지. 날짜가 나오면 요약에 기록 | 🟡 10턴마다 chat_latest 전체를 요약해 `summary_XXX_YYY.md`로 저장. L1→L2→L3를 DB에 통합하지만 **프롬프트에는 쓰이지 않음** (`getEffectiveSummaries` 미호출) | 4영역 구조로 재설계. L2/L3는 연결하거나 제거 |
| 요약 편집 | 장기 기억은 상시 편집. 단기 기억·관계도·목표는 갱신 직후 다음 메시지 전까지 편집·삭제 가능 (2026-09-11) [공식] | 🔌 `GET /summaries`, `POST /summarize`만 있고 UI와 편집 API는 없음 | **모든 영역 상시 편집**을 목표로 (§9 개선 포인트) |
| 장기 기억 회상 | 매 턴 장기 기억 중 **3개만** `<recalled_history>`로 주입 [추정, 역공학] | ❌ 최근 요약 파일 5개를 통째로 주입 | 임베딩 또는 키워드 기반으로 회상하고, **무엇을 회상했는지 UI에 표시** |
| 요약 동작 조건 | 서버에서 항상 동작 | 💤 요약·캐릭터 갱신이 레지스트리를 우회해 `ClaudeService`(API)를 직접 호출. **기본 설정(CLI, API 키 없음)에서는 조용히 실패**하고 chat_latest가 계속 커짐 (`MemoryService.kt:143-150`) | 요약도 `AiProviderRegistry`를 거치도록 (§8 P0) |
| 캐릭터 문서 자동 갱신 | 없음. 관계도 탭이 이 역할 | 🟡 요약할 때마다 **모든 캐릭터에 순차적으로 LLM 호출**(마도생존기 기준 약 25회). 문서는 계속 커지기만 함 | 관계도·장기 기억으로 흡수하거나, 등장한 캐릭터만 갱신 |
| **유저노트** | 캐릭터가 꼭 기억할 정보. 작품별 또는 채팅방 기본 설정. **500자 무료, 확장 시 2000자** [공식]. OOC나 `!` 명령 대신 권장 | 🔌 `must_remember.md`가 역할은 같음(프롬프트 ⑥번 주입). **편집 UI 없음** | 채팅 화면 사이드 패널에서 편집. 글자 수 제한 없음(개인용 장점) |
| **대화 프로필 (페르소나)** | 채팅방마다 지정. **최대 100개, 프로필당 500자** (2026-09-03) [공식] | 🟡 `protagonist.md` 1개(시나리오 단위, 스토리 복사본 우선). 요약할 때 LLM이 이 파일도 자동으로 고침 | 유저 단위 프로필 라이브러리로 분리하고 스토리 생성 시 선택. **자동 수정은 중단** |
| **키워드북 (로어북)** | 대화에 키워드가 나오면 노트 주입. 최대 20개, 노트당 400자, **동시 3개**, 위에 있는 것 우선. 시작 설정마다 별도 [공식] | ❌ 대신 **캐릭터 문서 전부를 매 턴 주입**(마도생존기 24명, 약 140KB). 활성 캐릭터 판별 `ContextService`는 💤 호출되지 않음 | 키워드북 도입과 함께 **캐릭터 문서도 키워드(이름)로 주입**. 토큰 비용이 크게 줄어듦 |
| 컨텍스트 / 토큰 관리 | 비공개 | ❌ 토큰 계산 없음. 출력 maxTokens 2048(API만) | 토큰 예산제(섹션별 한도와 자르기 우선순위) |
| 다른 서비스 대화 가져오기 | 정리 노트를 첫 메시지에 넣고 장기 기억에 수동 입력하는 가이드 [공식] | ❌ | 마크다운 로그 가져오기 + 자동 요약(파일 기반이라 쉬움) |

## 3. 시작 / 연출 / 출력 형식

| 기능 | 크랙 | crack-clone 현재 | 차이 / 할 일 |
|---|---|---|---|
| **시작 설정** | 작품당 여러 개. 각각 **프롤로그**(첫 메시지), 시작 상황, **플레이 가이드**(유저에게만 보이고 AI는 기억하지 않음), **추천 답변 3개**. 스탯·키워드북·미디어도 시작 설정마다 따로 [공식] | ❌ `scenario.md` 1개가 시스템 프롬프트에만 들어감. 새 스토리는 빈 화면 (`ChatPage.tsx:446-453`) | 스토리 생성 시 시작 설정 선택 → 프롤로그를 첫 AI 메시지로 기록 |
| **추천 답변** | AI가 맥락을 보고 후보 최대 3개 제안. 토글로 켜고 끔. 눌러서 바로 보내거나 수정 [공식] | ❌ | 응답이 끝나면 경량 모델로 3개 생성(비동기) |
| 상황 묘사 입력 | `**...**` = 상황 묘사, `"..."` = 대사. 상황추가 버튼 [공식] | 🟡 토글이 입력을 `**...**`로 감쌈. 그러나 **프롬프트는 `**`의 의미를 설명하지 않음** (`PromptAssembler.kt:25`) | BASE_RULE에 입력 규칙 추가 |
| **유저 사칭 방지** | 채팅방별 토글. AI가 유저의 대사·행동·생각을 쓰지 않음 (2026-09-17) [공식] | 🟡 출력 규칙에 "주인공 대사를 대신 쓰지 않기"가 고정으로 들어 있음 | 토글로 만들기(자동 진행 스타일을 원할 때 끌 수 있게) |
| 감정 표현 | 별도 기능 없음. 이미지 배치표의 '분류_상황'으로 표현 [추정] | 🟡 `[감정: …]` 태그를 강제하지만 **프론트는 태그를 지우기만** 함. MessageParser도 쓰이지 않고 둥근 따옴표(“”)를 대사로 인식하지 못함 | 감정 뱃지 표시에 활용하거나 태그 제거 |
| **상태창** | 전용 UI 없음. 제작자가 코드블록 마크다운 양식을 지시하고 답변 **하단**에 두기를 권장 [공식] | ❌ | 1단계: 코드블록을 카드로 렌더링. 2단계: 스탯과 연동 |
| **스탯** | 시작 설정당 최대 7개. 상단 게이지와 애니메이션. 레벨 4구간마다 100자 행동 프롬프트. 리롤과 연동 (2026-02) [공식] | 💤 `character_states` DB와 CRUD는 있음. **자동 추출이 없고** `scenarioId=0`이 하드코딩됨. UI 없음 | 응답 후 경량 모델로 스탯 변화를 추출해 DB에 저장하고 게이지로 표시 |
| **엔딩** | 10턴 이후 5턴마다 조건 검사(턴 수와 스탯, AND/OR). 에필로그 카드 N~SSR 등급 수집 [공식] | ❌ | 스탯 이후. 게임성이 강해 포트폴리오 데모용으로 좋음 |
| 예시 대화 | 최대 3개 (가이드라인 역할) [공식] | 🟡 출력 형식에 예시 장면 1개가 하드코딩됨 | 시나리오별 예시 대화 필드 |
| 프롬프트 템플릿 | 기본 / 1:1 롤플레잉 / 시뮬레이션 / 생산성 / 제작자 커스텀 [공식] | 🔌 `scenario_settings`(OUTPUT_TEMPLATE, VOLUME_RULE 등)의 CRUD만 있고 **PromptAssembler에 연결되지 않음** | 연결하고 템플릿 프리셋을 만들어 ScenarioDetailPage에 설정 탭 추가 |
| **단축어** | 작품당 20개와 개인 20개. 입력창에서 `/`로 호출. 단축어 허브에서 공유 [공식] | ❌ | `/일기`, `/플래시백` 같은 개인 단축어. 구현이 쉽고 체감이 큼 |
| 문체 설정 | 예고됨, 미출시로 보임 [공식+추정] | ❌ | 프롬프트 템플릿에 포함 |

## 4. 모델 / 생성 옵션

| 기능 | 크랙 | crack-clone 현재 | 차이 / 할 일 |
|---|---|---|---|
| **대화 모드 (모델 선택)** | 파워챗(25) ~ 페이블챗(230 크래커)까지 11종. Gemini·Claude 계열. 채팅방마다 선택하고 기본값 저장 [공식] | 🟡 프로바이더(claude-code-cli / claude-api)만 선택 가능. 본 응답은 OPUS tier로 고정. **CLI는 tier를 무시**하고 `--model`을 넘기지 않음. 설정의 모델 ID가 구버전(`claude-opus-4-20250514` 등) | Plan-features의 Feature 1(턴별 모델 선택) 완성. 모델 ID 최신화. CLI에 `--model` 전달 |
| **답변 최대 길이** | 1000 / 1500 / 3000 / 5000 토큰. 제작자 권장값 제공 [공식] | ❌ (API는 2048 고정, 프롬프트는 "최소 200자") | 채팅방 설정에서 길이 선택 |
| **생각 깊이** | 끄기 / 기본 / 깊게 / 더 깊게. 추론 토큰도 출력 한도에 포함 (2026-09-17) [공식] | ❌ | API 프로바이더에 extended thinking 옵션 |
| 토큰 단위 스트리밍 | ✅ | 🟡 CLI에 `--include-partial-messages`가 없어 **완성본이 한 번에 옴**. 프롬프트를 argv로 넘겨 ARG_MAX 위험이 있고, 경로가 하드코딩됨 | CLI 스트리밍 옵션 추가, 프롬프트는 stdin으로 전달 |
| 응답 저장 주체 | 서버 | 🟡 스트림이 끝나면 **프론트가 `/complete`로 전체 응답을 다시 전송**해 저장. 클라이언트가 끊기면 응답이 사라짐 | 서버가 스트림 종료 시 직접 저장 (§8 P0) |
| 동시 생성 제한 | 계정당 1개 [공식] | ❌ | 개인용이라 불필요. 스토리별 중복 요청만 막으면 됨 |

## 5. 채팅방 / 기타

| 기능 | 크랙 | crack-clone 현재 | 비고 |
|---|---|---|---|
| 채팅방 여러 개 | 작품당 여러 방, 고정 5개, 보관함, 자동 정리 [공식] | ✅ 스토리 = 채팅방. 🔌 archive API는 있지만 UI 없음 | 고정과 보관 UI 추가 |
| 채팅방 기본 설정 | 새 방에 적용할 프로필·유저노트·출력량·모드 기본값 [공식] | ❌ | 프로필 라이브러리와 함께 |
| 채팅방 UI 테마 | **소설형 UI**와 채팅형, 글꼴 설정, 다크모드 [공식] | 🟡 채팅형만. 모바일 레이아웃과 PWA 있음 | 소설형 뷰는 포트폴리오에서 눈에 잘 띄는 기능 |
| 대화 내보내기 | 공식 기능 없음(비공식 다운로더 존재) [공식] | 🟡 원본이 마크다운 파일이라 사실상 이미 보유 | **크랙보다 나은 점.** 다운로드 버튼만 추가하면 됨 |
| 실시간 상황 이미지 생성 | 대화 맥락으로 삽화 생성, 재생성, 보관함 (2025-09) [공식] | ❌ | 후순위. 외부 이미지 API 필요 |
| 상황 이미지 (제작자 등록) | '분류_상황' 배치표 최대 1600장. AI가 골라서 표시 [공식] | ❌ (Plan-features의 Feature 2로 계획만 있음) | Feature 2 설계를 배치표 방식으로 수정 |
| 파티챗 | 2~5인 턴제. AI가 모든 입력을 종합 [공식] | ❌ | 개인용 범위 밖. 기술 데모로만 고려 |
| TTS 목소리 | **2026-02-09 운영 종료** [공식] | ❌ | 제외 |
| 크래커, 세이프티 등급, 성인인증, 단축어 허브, 크리에이터 정산 | 상용 요소 | — | **제외** |

---

## 6. 크랙보다 이미 나은 점 / 차별점으로 키울 점

포트폴리오에서 "단순 클론"이 아니라 **설계 판단**을 보여줄 수 있는 지점이다.

1. **투명한 파일 기반 기억.** 모든 대화·요약·캐릭터 문서가 마크다운이라 사용자가 직접 읽고 고칠 수 있다. 크랙의 가장 큰 불만(기억력, 편집 불가)에 대한 구조적 답이다.
2. **캐릭터 Copy-on-Write.** 스토리별로 진화한 캐릭터 문서를 따로 두는 상속 구조로, 크랙에 대응 기능이 없다.
3. **프로바이더 추상화.** CLI(구독, 비용 0)와 API를 교체할 수 있다.
4. **내보내기 기본 제공.** 크랙은 공식 내보내기가 없다.

**크랙 사용자 불만에서 뽑은 개선 목표** ([커뮤니티] 나무위키, 디시 뤼튼갤 등)

| 크랙의 불만 | 우리 목표 |
|---|---|
| 기억력: 50~100턴 이후 설정 불일치, 반복 인사, 존댓말·반말 무시 | 요약 4영역 **전부 상시 편집** + **고정(pin) 기억** + 매 턴 회상된 기억을 UI에 표시 |
| OOC 지시가 약 20턴 뒤 잊힘 | **작가 노트**: 지속 지시를 최근 N턴 위치(depth)에 주입 |
| 페르소나 100자, 유저노트 500자 제한과 확장 과금 | 제한 없음. 토큰 예산 안에서 자동 경고 |
| 분기가 방 복제뿐이고, 재생성 비교 UI 불명확 | **트리형 분기 뷰** + 재생성 **스와이프 비교** |
| 모델, 출력량 정책이 불투명 | 모델과 토큰 사용량을 턴마다 표시(디버그 패널) |
| 서드파티 기억 보조 도구가 성행(Gemini로 요약해 유저노트에 주입) | 이 흐름을 **기본 기능**으로 내장 |

---

## 7. 현재 코드의 정합성 문제 (새 기능보다 먼저 해결)

코드 인벤토리에서 확인한 버그로, 새 기능이 모두 이 위에 올라가므로 선행 과제다.

1. **요약 후 인덱스 어긋남과 이중 요약.** 10턴 요약으로 chat_latest가 비워져도 프론트 state는 옛 메시지를 유지한다. 이때 재생성을 누르면 turnCount만 감소하고, 다시 10턴이 되면서 `summary_001_010`과 아카이브를 **거의 빈 내용으로 덮어쓴다** (`ChatService.kt:131`, `MemoryService.kt:95-122`).
2. **삭제·분기 시 turnCount 초기화.** chat_latest 안의 assistant 수로만 다시 세기 때문에 요약된 턴이 사라지고 이후 파일명이 꼬인다 (`ChatService.kt:118`, `StoryService.kt:99`).
3. **수동 `/summarize` 범위 충돌.** 15턴에 실행하면 `11~20`으로 저장되고, 20턴 자동 요약이 같은 파일을 덮어쓴다.
4. **기본 설정에서 기억 시스템 전체가 꺼져 있음.** API 키가 없으면 요약·캐릭터 갱신·L2/L3가 조용히 실패한다.
5. **응답 유실 경로.** 재생성은 원본을 먼저 삭제하고, 저장은 클라이언트의 `/complete`에 의존하며, 전송이 실패하면 유저 메시지만 남아 이후 유저 메시지가 연속된다.
6. **죽은 코드.** `ContextService`, `ChatMessage` 엔티티·리포지토리, `getEffectiveSummaries`, `getSettingValueOrDefault`가 호출되지 않는다. `StateController`는 `scenarioId=0`이다.
7. 기존 BUG-005(dataPath를 DB에 저장해 경로가 어긋남)가 아직 임시 조치 상태다.

> 근본 원인은 대화를 **파일 + 배열 인덱스**로 다루는 구조다. 메시지마다 **안정적인 ID와 전역 턴 번호**를 부여하는 저장 모델로 바꾸는 것이 1~3, 5와 재생성 스와이프·트리 분기의 공통 해법이다. 파일을 원본으로 유지할지, 대화만 DB로 옮기고 파일은 내보내기용으로 둘지는 이 단계에서 결정한다.

---

## 8. 로드맵 제안

> ⚠️ **대체됨.** 위 피드백과 Q&A 결정을 반영한 확정 로드맵은 [Plan-roadmap.md](./Plan-roadmap.md)에 있습니다. 아래는 조사 직후의 초안으로, 기록용으로만 남겨 둡니다.

### P0 — 대화 기반 정비 (선행 필수)
- [ ] 메시지 저장 모델 재설계: 메시지 ID, 전역 턴 번호, 재생성 후보(variants) 필드
- [ ] 스트림이 끝나면 **서버가 저장**. 재생성은 성공한 뒤에 교체
- [ ] 요약·분석 호출을 `AiProviderRegistry` 경유로 바꿔 CLI 기본 설정에서도 기억 시스템이 동작하게
- [ ] 요약 범위를 턴 번호 기반으로 계산(수동과 자동 충돌 제거). 분기 시 기억 전체 복사
- [ ] 이어쓰기를 가상 지시로 바꿔 히스토리 오염 제거
- [ ] 죽은 코드 정리(연결하거나 삭제), BUG-005 근본 수정

### P1 — 크랙 핵심 대화 경험
- [ ] **요약 메모리 4영역**(단기 / 장기 / 관계도 / 목표) + 편집 UI + 갱신 뱃지
- [ ] **유저노트**(must_remember 편집 UI) + **대화 프로필 라이브러리**
- [ ] **시작 설정**: 프롤로그(첫 메시지), 시작 상황, 플레이 가이드
- [ ] **추천 답변** 3개(토글, 수정 후 전송)
- [ ] **재생성 스와이프**, 유저 메시지 수정, 재생성 지시
- [ ] **키워드북** + 캐릭터 문서를 키워드로 주입(토큰 절감)
- [ ] **모델 선택 + 답변 최대 길이**(Feature 1 완성, 모델 ID 최신화, CLI `--model`)
- [ ] 유저 사칭 방지 토글, `**상황**` 입력 규칙을 프롬프트에 명시

### P2 — 게임성과 연출
- [ ] **스탯**: 응답 후 자동 추출, 게이지, 레벨별 행동 프롬프트(state 도메인 재활용)
- [ ] 상태창 코드블록을 카드로 렌더링
- [ ] **단축어**(`/` 명령)
- [ ] 예시 대화, 프롬프트 템플릿(`scenario_settings` 연결)
- [ ] **엔딩** 조건과 에필로그 카드
- [ ] 생각 깊이(extended thinking)

### P3 — 차별화와 확장
- [ ] **장기 기억 회상(RAG)** + 회상 내역 표시 + 고정 기억
- [ ] **트리형 분기 뷰**
- [ ] 작가 노트(depth 주입), 토큰 예산제와 사용량 패널
- [ ] 소설형 UI, 대화 내보내기 버튼, 외부 로그 가져오기
- [ ] 상황 이미지(배치표 방식으로 Feature 2 수정) → 실시간 삽화 생성
- [ ] (선택) 파티챗 기술 데모

### 제외
크래커·결제, 세이프티 등급·성인인증, 단축어 허브와 공유 마켓, 크리에이터 정산, TTS(원작도 종료), 동시 생성 제한.

---

## 9. 확인하지 못한 항목 (크랙 쪽)
- 재생성 후보를 좌우로 넘기는 UI인지, 재생성 비용
- 삭제가 해당 메시지만 지우는지, 그 뒤까지 지우는지
- 키워드북 스캔 범위(최근 몇 턴)와 매칭 방식
- 컨텍스트에 포함되는 최근 대화 턴 수, 장기 기억 회상 기준
- 하이퍼챗 3.0과 상황 이미지 생성의 현재 가격(범위 밖이라 영향 없음)

## 10. 주요 출처
- 공식 고객센터: https://help.crack.wrtn.ai/ (채팅 가이드 `/crackguide/chat`, 스토리 빌더 가이드 `/guide/user/tutorial/story/1~8`, 대화 이어오기 `/load`)
- 공식 공지 (`https://crack.wrtn.ai/announcement/{id}`)
  - 요약 메모리 개편 `6982dc234516bd881cce198e` · 요약 메모리 수정 `6aa3ce2cfea6fed6669ae47b`
  - 분기 및 일반챗 종료 `69bd00ca30c8f30c6c61ae47` · 대화 프로필 확장 `6a99245e66714009d0472a31`
  - 유저노트 확장 `6848cdd81ac53aa20a4c9b92` · 스탯 개편 `6981bf082570c3a53df63236`
  - 생각 깊이 및 유저 사칭 방지 `6aab731094fbb132d698a8f8` · 9월 업데이트 예고 `6a954af0a625bf2a371065f2`
  - 파티챗 `690426aa539ff1557c4bb9a0` · 상황 이미지 생성 `68c13bc3d37781af5241710c`
  - 목소리 재생 출시 `6874c53015d8d6a78e7232ad` · 웹 단축키 `68d5fbdb355bd573b811ad9d`
- 공지 원문 API(인증 불필요): `https://crack-api.wrtn.ai/crack-api/announcements?page=N&limit=50`
- 나무위키 「크랙(애플리케이션)」 (최종수정 2026-09-21), 디시 뤼튼 마이너갤 공략·역공학 글(`no=961451`, `747571`, `747946`, `294641`), AI채팅갤 장기 비교 후기(`no=84472`)
