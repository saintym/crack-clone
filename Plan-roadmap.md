# crack-clone 로드맵 (2026-09-22 확정)

> **실행 계획:** 기술 설계는 [docs/DESIGN.md](./docs/DESIGN.md), 작업 단위와 진행 상황은 [docs/tasks/](./docs/tasks/README.md)에 있다.
> 근거 문서: [Plan-crack-gap.md](./Plan-crack-gap.md) (크랙 기능 조사와 현재 코드 격차) + 그 문서에 적은 기획 피드백 + 이후 Q&A 결정.
> 원칙: **기억력과 OOC 지시 유지가 이 프로젝트의 핵심 가치다.** 크랙에서 가장 크게 느낀 불만이 바로 이 두 가지다.

---

## 1. 결정 사항

| # | 주제 | 결정 |
|---|---|---|
| D1 | 모델 선택 | 지금처럼 **CLI / API 프로바이더 선택만** 둔다. 모드와 모델을 턴마다 고르는 기능은 만들지 않는다 |
| D2 | 메시지 수정 | **유저·AI 메시지 모두 수정 가능.** 이후 진행은 수정된 내용을 기준으로 한다 |
| D3 | 과거 메시지 수정과 기억 | 이미 기억으로 기록된 턴을 고치면 "수정됨" 표시만 해 둔다. **다음 기억 기록 때** 관리자가 그 턴을 다시 읽어 기억을 고친다. 플레이는 끊지 않는다 |
| D4 | 분기 | **후순위.** 지금 기능은 유지하고 확장하지 않는다 |
| D5 | 기억 구조 | 크랙의 4탭을 그대로 따라 하지 않는다. **인물 문서 · 주인공 문서 · 시나리오 연대기**의 3계층으로 고도화한다 (§2) |
| D6 | 기억 기록 시점 | **10턴마다 자동**, 또는 **`/기록` 명령**으로 수동. 매 턴 실행하지 않는다 |
| D7 | 기억 반영 방식 | **배경에서 자동 반영**하고 채팅 화면에는 작은 뱃지만 띄운다. 승인 요청이나 diff 팝업 등 **몰입을 깨는 것은 금지.** 회차별 변경 이력은 기억 패널에서 보고, 회차 단위로 되돌릴 수 있다 |
| D8 | 관리자 에이전트 | **응답 전에는 문서만 읽는다**(LLM 호출 없음, 대기 시간 0). 캐릭터·시나리오 관리자는 **기록 시점에만** 병렬로 실행된다 |
| D9 | 인물 문서 주입 | **키워드 + 동행 목록.** 최근 N턴에 이름이나 별칭이 나온 인물과, 시나리오 관리자가 관리하는 "현재 동행 인물"만 넣는다 |
| D10 | OOC | **지속 지시 목록.** `/ooc 내용`으로 추가하면 해제할 때까지 매 턴 프롬프트 **맨 아래**(가장 강하게 반영되는 위치)에 들어간다. UI에서 목록을 보고 끌 수 있다 |
| D11 | '/' 기능 | **`/명령`**(유저가 호출)과 **키워드북**(키워드가 나오면 자동 주입)을 **둘 다** 만든다 |
| D12 | 스토리 격리 | 스토리를 시작하면 **원본 문서를 통째로 복사**한다. 이후 시나리오 원본을 고쳐도 **진행 중인 스토리에는 영향이 없다**(새 스토리에만 반영). 같은 시나리오의 스토리끼리 서로 영향을 주지 않는다는 원칙은 **critical** |
| D13 | 상태 UI | 인물 상태 패널은 만든다. 다만 **기억 기록 때만 갱신**한다. 엔딩이나 매 턴 바뀌는 수치는 만들지 않는다 |
| D14 | 이미지 | 시나리오에 **태그 → 외부 URL**(webp 등)을 미리 등록하고, AI가 장면에 맞는 태그를 골라 응답에 넣는다. 프론트는 URL을 받아와 표시한다. 이미지 생성은 하지 않는다 |
| D15 | 파티챗 | 만들지 않는다 |
| D16 | 첫 메시지(프롤로그) | **도입한다.** 시나리오의 `prologue.md`를 스토리 시작 시 첫 AI 메시지(턴 0)로 넣는다. 수정 가능 (2026-09-23) |
| D17 | 재생성 이전 답변 | **보관한다.** 재생성할 때마다 후보로 쌓고 ‹ n/m ›로 넘겨 고른다. 재생성이 실패하면 원본을 유지한다 (2026-09-23) |
| D18 | 메시지 저장소 | **DB로 옮긴다.** 메시지마다 ID, 턴 번호, 후보, 수정 표시를 둔다. 인물·연대기 같은 기억 문서는 마크다운 파일로 유지하고, 대화는 마크다운으로 내보낼 수 있게 한다 (2026-09-23) |
| D19 | 감정 태그 | AI 출력 첫 줄의 `[감정: …]`은 유지한다. 출력 품질과 이미지 선택을 돕는 **내부 신호**다. **사용자에게는 절대 보이지 않게** 서버가 스트리밍 단계에서 떼어 내 따로 저장한다 (2026-09-23) |
| D20 | 삭제 | 메시지를 지우면 그 메시지부터 끝까지 모두 지워진다. 기억으로 기록된 턴까지 지우면 해당 기억 기록을 최근 것부터 자동으로 되돌린다 (2026-09-23) |
| D21 | 재생성 범위 | **가장 최근 AI 답변만** 재생성하고 후보를 고를 수 있다 (2026-09-23) |
| D22 | 세계관 관리자의 시나리오 지식 | 스토리를 시작할 때 `scenario.md` 원문을 **한 번만** 읽어 ① 다이제스트(항상 주입, 예산 3,000자)와 ② **색인된 조각**(주제 단위 + 키워드)을 만든다. 이후 원문은 다시 읽지 않는다. 관리자의 일은 연대기에 사건을 덧붙이는 것과, 연대기를 압축할 때 다이제스트를 갱신하는 것뿐이다 (2026-09-24) |
| D23 | 과거 정보 제공 방식 | 관리자에게 매 턴 묻지 않는다. 색인된 조각을 **키워드 검색으로 주입**한다(LLM 호출 0, 대기 0). 키워드북과 같은 매칭 엔진을 쓴다 (2026-09-24) |
| D24 | 깊은 질의 | 검색이 아무것도 못 찾았고 **과거를 참조하는 신호**가 있는 턴에만, 마스터가 세계관 관리자를 1회 자동 호출한다. 호출 조건은 좁게 유지하고 결과는 그 턴 컨텍스트에 넣는다 (2026-09-24) |
| D25 | 대화 원문 범위 | **턴 수가 아니라 글자 예산**으로 정한다. 기본 6,000자, 상한 10,000자, 최소 2턴 보장. 최근 턴부터 예산까지 담고 그 이전은 연대기 요약으로 덮는다. 응답 길이가 76~2,282자로 들쭉날쭉해서 턴 수 기준은 예측이 안 된다 (2026-09-24 수정) |
| D34 | 인물 이미지 위치 | 인물 이미지를 말풍선 맨 위 한 장에서 **본문 인라인**으로 옮긴다. AI가 `{{img:이름_변형}}`을 그 인물의 대사·등장 묘사 **바로 앞 줄**에 넣고, 말하는 인물이 바뀔 때마다 새로 넣는다(한 응답에 3개까지). D31은 첫 줄 `[인물: …]` 태그 하나로 골라 맨 위에 붙였는데, 실제로 플레이하니 **이미지가 먼저 나오고 그 아래로 소설이 흐르는** 모양이 됐다. 800~1,500자(D33) 안에서 화자가 두세 번 바뀌는데 얼굴은 하나뿐이라 누가 말하는지와 보이는 얼굴이 어긋났다. 첫 줄 태그와 `speaker` 컬럼은 **폴백**으로 남긴다(본문 태그가 하나도 없는 턴, D34 이전 메시지, `prologue.md`). 상한을 3개로 둔 건 대사가 오가는 턴에서 화면이 이미지로 도배되는 것을 막기 위해서다 (2026-09-27) |
| D33 | 응답 분량 | 한 응답 목표를 **800~1,500자**로 프롬프트에 명시한다(상한 2,000자). 기존 "최소 200자"만 있던 규칙이 응답을 계속 길어지게 했다(실측 720자 → 2,032자, 지연 17.5초 → 35.5초). 전역 기본값을 설정에 두고, 시나리오별로 `settings.json`에서 조정한다 (2026-09-26) |
| D32 | URL에서 시나리오 가져오기 | 설정이 정리된 웹페이지 URL을 주면 AI가 읽어 시나리오 한 벌(세계관·시나리오·인물 전원·주인공·첫 메시지·키워드북·이미지 카탈로그)을 만든다. 페이지마다 형식이 달라 규칙 파싱이 아니라 LLM이 옮긴다. 페이지에 없는 정보(주인공 등)는 **가져오기 중간에 사용자에게 질문**한다. 인물 이미지 URL은 `{이름}_기본`으로 자동 등록한다 (2026-09-25) |
| D31 | 인물 이미지 표시 | 인물이 등장하는 턴에는 **거의 항상** 이미지가 나와야 한다. AI 재량에 맡기지 않고, AI가 첫 줄에 `[인물: 이름/변형]`을 쓰되 **변형은 프롬프트에 준 카탈로그 목록 안에서 고른다**(카탈로그에 기본·당황·분노만 있으면 슬픔일 때도 그중 가까운 것). 프론트가 렌더 시 `{인물}_{변형}` → `{인물}_기본` 순으로 찾는다. AI가 본문에 직접 쓴 `{{img:태그}}`가 있으면 그게 우선이다. 업로드 기능은 만들지 않는다. 사용자가 앱 밖에 올리고 URL만 등록한다 (2026-09-25) |
| D29 | 세션 유지 | **하지 않는다.** CLI를 띄워 두고 턴만 넣는 방식(`--input-format stream-json`, `--resume`)은 가능하지만, 실측에서 이득이 없었다. CLI는 이미 프롬프트 캐싱을 적용하고 **프로세스가 달라도 접두사가 같으면 캐시를 읽는다**(2회차 캐시 읽기 42,856토큰, 비용 $0.2565 → $0.0161). 지연도 프롬프트 3KB와 85KB가 같았다. 세션을 쓰면 오래된 원문 정리·수정·삭제·분기가 깨지므로 얻는 것보다 잃는 것이 크다 (2026-09-24) |
| D30 | 활성 인물 집합 고정 | 캐시는 접두사 일치이므로 **매 턴 활성 인물이 바뀌면 캐시가 깨진다**(비용 16배). 활성 인물 집합을 **기억 기록 회차 동안 고정**하고, 새 인물이 언급되거나 기록이 돌 때만 바꾼다 (2026-09-24) |
| D28 | 프롬프트 캐시 친화 순서 | Messages API는 상태가 없어 매 턴 맥락을 다시 보낸다. 캐싱은 **접두사 일치**이므로 안정적인 섹션(기본 규칙 → world → 다이제스트)을 앞에, 매 턴 바뀌는 섹션(활성 인물 → 키워드 조각 → 최근 대화 → 지시)을 뒤에 둔다. API 프로바이더에서는 다이제스트 끝에 캐시 지점을 둔다. CLI(구독)는 과금이 아니라 **지연** 이득이므로 효과를 측정해 기록한다 (2026-09-24) |
| D26 | 롤플레이 마스터 역할 | 매 턴 응답 호출을 **롤플레이 마스터**로 프롬프트에 명시한다. 주인공 주변의 시간·공간·인물을 고려해 사건을 진행시키는 책임, 그리고 정보 출처(다이제스트·연대기·인물 문서)를 규칙에 적는다 (2026-09-24) |
| D27 | 오프스크린 캐릭터 | 주인공과 떨어진 곳의 인물이 스스로 계획·행동하는 기능은 **사용자 테스트가 끝난 뒤** 착수한다 (2026-09-24) |

---

## 2. 기억 시스템 v2 설계

> 개념 설계다. 파일 이름, 스키마, API 같은 정확한 계약은 [docs/DESIGN.md](./docs/DESIGN.md)가 기준이다.

### 2.1 스토리 폴더 구조 (D12: 시작할 때 통째로 복사)

```
data/{시나리오}/                      ← 원본(템플릿). 플레이 중에는 읽지 않음
  world.md, scenario.md, characters/*.md, keywords.md, commands.md, images.md

data/{시나리오}/stories/{id}/         ← 스토리. 플레이 중에는 여기만 읽고 씀
  world.md, scenario.md             ← 복사본
  characters/{이름}.md               ← 복사본 + "## 기억" 섹션 (3-1)
  characters/protagonist.md         ← 복사본 + "## 변화 기록" 섹션 (3-2)
  chronicle.md                      ← 시나리오 연대기 (3-3)
  keywords.md, commands.md          ← 키워드북·명령 복사본 (스토리마다 수정 가능)
  user_note.md                      ← 유저노트 (기존 must_remember)
  directives.json                   ← 지속 OOC 지시 목록 (D10)
  state.json                        ← 현재 동행 인물, 위치, 시간
  memory/history/{기록ID}/           ← 기록 직전 스냅샷 (D7 되돌리기용)
  (대화 메시지는 DB에 저장, D18)
```

- `images.md`처럼 스토리에서 바뀌지 않는 카탈로그는 원본을 참조해도 된다. **바뀔 수 있는 문서는 전부 복사한다.**
- 기존 스토리는 마이그레이션한다. 원본 문서를 복사하고, 지금 있는 스토리별 캐릭터 파일과 요약은 새 구조로 병합한다.

### 2.2 문서별 기록 규칙

| 문서 | 담는 것 | 담지 않는 것 |
|---|---|---|
| **인물 문서** `## 기억` (3-1) | 주인공이나 다른 인물과의 **관계에 영향을 준 큰 사건**, 감정 변화, **영구적인** 소지품·기술·신체 변화 | 일상 대화, 일시적 감정, 사소한 행동 |
| **주인공 문서** `## 변화 기록` (3-2) | 인물 문서에 기록된 사건 가운데 주인공 쪽 변화: 인물별 관계 변화, 스탯·기술·소지품·신체 변화 | 인물 쪽 속마음(인물 문서에만 둠) |
| **연대기** `chronicle.md` (3-3) | 회차별 사건 흐름(시간, 장소, 무슨 일). 길어지면 오래된 부분을 **장(章) 요약**으로 압축 | 대사 원문 |

- 모든 기억 섹션은 **정해진 마크다운 제목 구조**를 따른다(예: `### 관계`, `### 소지품`, `### 기술`, `### 사건`). 상태 패널(D13)이 이 구조를 파싱해서 보여준다.
- 문서마다 **길이 예산**을 둔다. 넘으면 관리자가 오래된 항목을 합쳐 압축한다. 기억 문서가 계속 커지기만 하는 지금의 문제를 막기 위한 장치다.

### 2.3 기억 기록 파이프라인 (D6·D8, 배경 실행)

```
트리거: 10턴 도달 또는 /기록  (마지막 기록 이후 턴 + 수정됨 표시된 턴)
   │
   ▼
① 시나리오 관리자 (1회 호출)
   · 연대기에 이번 회차 추가
   · 동행 인물·위치 갱신 → state.json
   · "큰 변화가 있었던 인물" 목록 산출
   │
   ▼
② 캐릭터 관리자 × N (해당 인물만, 병렬, 동시 실행 수 제한)
   · 각자 자기 인물 문서의 ## 기억 갱신 (기록 기준은 §2.2)
   · 주인공 쪽 변화 항목을 결과로 반환
   │
   ▼
③ 주인공 반영 (1회 호출)
   · ②의 결과를 합쳐 protagonist.md ## 변화 기록 갱신
   │
   ▼
④ 변경 전후 스냅샷을 memory/history/에 저장 → 채팅 화면에 기억 뱃지 표시
```

- 실패하면 해당 회차만 다시 시도한다. **부분 반영은 하지 않는다**(회차 단위로 원자적 반영).
- 모든 호출은 `AiProviderRegistry`를 거친다. CLI 기본 설정에서도 동작해야 한다(지금 있는 버그 수정).
- 기록이 도는 동안에도 플레이는 계속된다. 기록 대상 턴 범위는 트리거 시점에 고정한다.

### 2.4 매 턴 프롬프트 조립 (LLM 사전 호출 없음)

```
[시스템]
 1. 기본 규칙 + 출력 형식 (+ 유저 입력 규칙: **상황**, "대사")
 2. world.md
 3. scenario.md + 연대기 (최근 회차 원문 + 오래된 장 요약)
 4. protagonist.md
 5. 활성 인물 문서 (D9: 동행 목록 ∪ 최근 N턴 키워드 매칭)
 6. 키워드북 매칭 항목 (D11)
 7. 유저노트
 8. 사용 가능한 이미지 태그 목록 (D14)
[대화]
 9. 마지막 기록 이후 원문 + 겹치는 몇 턴
[하단 — 가장 강하게 반영되는 위치]
10. 지속 OOC 지시 목록 (D10)
11. 유저 메시지
```

- **측정 지표(포트폴리오용):** 지금은 매 턴 시스템 프롬프트가 약 190KB입니다(마도생존기 기준, 인물 24명 140KB + scenario·world 49KB). v2 전후의 프롬프트 크기와 응답 지연을 비교해 기록합니다.
- **T13 측정 (2026-09-23, 시스템 프롬프트만, 대화 원문 제외):**

  | 대상 | v1 (모든 인물) | v2 | 감소 |
  |---|---|---|---|
  | 마도생존기 원본 폴더, 최근 대화에 인물 언급 없음 | 192,013 B (85,837자) | 56,220 B (24,895자) | −71% |
  | 마도생존기 스토리 대화 기준(최근 대화에서 설월·소율·위지연 3명 매칭) | 192,013 B | 79,303 B (35,502자) | −59% |
  | sample-scenario 픽스처(인물 2명, 각 약 150자) | 1,560자 | 1,546자(0명) / 1,703자(1명) / 1,856자(2명) | 인물이 작아 차이 없음 |

  - v2 섹션(마도생존기, 3명 매칭): base 1,128 · world 3,653 · scenario 17,698 · protagonist 2,315 · characters 10,605 · user_note 93 (글자 수). 인물 24명 전체는 60,793자다.
  - v2의 base는 유저 입력 규칙(`**…**`, `"…"`, `[지시]`)을 더해 v1보다 약 300자 길다. 픽스처처럼 인물 문서가 작으면 전체 크기는 v1과 비슷하다.
  - 실제 데이터는 아직 v2 구조로 이전(T09)하기 전이라, 원본 폴더의 문서를 스토리 폴더처럼 읽고 각 스토리의 `chat_latest.md` 최근 메시지로 인물을 골라 쟀다. 대화 원문은 v2에서 최근 30턴(기록 후에는 마지막 기록 턴 − 2 이후)으로 제한된다. 응답 지연 비교는 아직 하지 않았다.
  - 다시 재려면 `GET /api/stories/{id}/prompt-preview`(섹션별 글자 수)나 조립 INFO 로그를 본다.

- **CLI 실측 (2026-09-24, `claude -p`에 직접 프롬프트를 넣어 측정)**

  | 입력 | 첫 토큰까지 | 전체 |
  |---|---|---|
  | 3,055자 | 3.40~5.75초 | 6.05~8.54초 |
  | 30,055자 | 3.22~4.84초 | 6.32~7.72초 |
  | 85,041자 | 4.34~4.75초 | 7.01~7.25초 |

  입력이 28배 커져도 지연은 사실상 같다. **지연을 좌우하는 것은 출력 길이다.**

  | 같은 프롬프트(60,000자 ≈ 43K 토큰) | 캐시 쓰기 | 캐시 읽기 | 비용 |
  |---|---|---|---|
  | 1회차 | 30,911 | 11,945 | $0.2565 |
  | 2회차(새 프로세스) | 0 | 42,856 | $0.0161 |

  프로세스가 달라도 접두사가 같으면 캐시를 읽는다(16배 저렴). 따라서 프롬프트를 줄이는 목적은 지연이 아니라 **① 출력 품질(필요한 정보에 집중) ② 접두사 안정성(캐시) ③ API 프로바이더 전환 시 비용**이다.

### 2.5 관리자 역할 구조 (2026-09-24)

지금도 기록 시점에 관리자들이 병렬로 돌고 있다. 아래는 각자의 책임을 정리한 것이다.

| 역할 | 언제 도는가 | 읽는 것 | 쓰는 것 |
|---|---|---|---|
| **세계관 관리자** | 스토리 생성 시 1회(색인 빌드) + 기록마다(연대기) + 압축 시(다이제스트 갱신) | `scenario.md`는 빌드 때만. 이후엔 대상 턴 원문과 연대기 | `digest.md`, `scenario_index.md`, `chronicle.md`, `state.json` |
| **캐릭터 관리자 × N** | 기록마다, 등장 인물만 병렬 | 자기 인물 문서 + 대상 턴 원문 | `characters/{이름}.md`의 `## 기억` |
| **주인공 반영** | 기록마다 1회 | 캐릭터 관리자 결과 + 주인공 문서 | `characters/protagonist.md`의 `## 변화 기록` |
| **롤플레이 마스터** | 매 턴 | 다이제스트 · 검색된 시나리오 조각 · 연대기 · 활성 인물 문서 · 최근 10턴 원문 · 지시 | 응답(대화) |

핵심은 **관리자에게 묻지 않고, 관리자가 미리 검색 가능한 형태로 펼쳐 둔 문서를 마스터가 읽는다**는 것이다(D23). 예외는 검색이 헛돈 턴의 1회 자동 질의(D24)다.

---

## 3. 단계별 목표

### M0 — 기반 정비 (선행 필수)
- [ ] 메시지 저장 모델 재설계: **메시지 ID + 전역 턴 번호 + 수정됨 표시.** 요약 후 인덱스가 어긋나거나 turnCount가 초기화되는 버그를 근본적으로 제거
- [ ] 스트림이 끝나면 **서버가 저장**. 재생성은 성공한 뒤에 교체. 이어쓰기는 가상 지시로 바꿔 히스토리를 오염시키지 않기
- [ ] 모든 AI 호출을 `AiProviderRegistry` 경유로. CLI에서 토큰 단위 스트리밍, 프롬프트는 stdin으로 전달, 경로 하드코딩 제거
- [ ] **스토리 시작 시 원본 통째로 복사**(D12) + 기존 스토리 마이그레이션 + BUG-005 근본 수정
- [ ] 죽은 코드 정리: ContextService, L2/L3 통합, scenario_settings, ChatMessage 엔티티. v2에 쓸 것만 남긴다

### M1 — 기억 시스템 v2 (핵심)
- [ ] 기억 문서 구조(§2.2)와 템플릿
- [ ] 기록 파이프라인(§2.3): 시나리오 관리자 → 캐릭터 관리자 병렬 → 주인공 반영
- [ ] 트리거: 10턴 자동 + `/기록`. 수정된 턴 재반영(D3)
- [ ] 회차별 변경 이력과 되돌리기, 기억 뱃지(D7)
- [ ] 문서 길이 예산과 압축, 연대기 장 요약
- [ ] PromptAssembler 재작성(§2.4): 활성 인물 선택(D9), 섹션 순서
- [ ] 기억 패널: 인물·주인공·연대기 문서를 보고 편집하는 화면
- [ ] v2 전후 프롬프트 크기 측정

### M2 — OOC와 '/' 기능
- [ ] `/` 명령 프레임워크: 입력창 자동완성, 시스템 명령과 사용자 정의 명령
  - 시스템 명령: `/기록`, `/ooc`
  - 사용자 정의 예: `/일기`, `/플래시백`
- [ ] 지속 지시 목록(D10): `/ooc` 추가, 패널에서 보기·끄기·삭제, 프롬프트 하단 주입
- [ ] 키워드북(D11): 키워드와 별칭 → 노트. 최근 N턴 스캔, 동시 발동 수 제한. 인물 문서 선택(D9)과 매칭 엔진 공유
- [ ] 유저노트 편집 UI

### M3 — 대화 조작 UX
- [ ] 유저·AI 메시지 수정 UI와 수정 반영(D2)
- [ ] 메시지 삭제를 턴 번호 기반으로 정리
- [ ] 재생성 후보 보관과 ‹ n/m › 선택, 재생성 지시(D17)
- [ ] 첫 메시지(프롤로그)(D16)

### M4 — 상태 UI와 이미지
- [ ] 인물 상태 패널(D13): 기억 섹션을 파싱해 관계·소지품·기술 표시. 기록 때만 갱신
- [ ] 이미지 카탈로그(D14): 태그 → URL 등록 UI, 프롬프트에 태그 목록 주입, 응답 속 태그를 파싱해 `<img>`로 표시

### M5 — 세계관 관리자 고도화 (2026-09-24 추가)
- [ ] 시나리오 다이제스트 + 색인 조각 빌드(D22), 키워드 검색 주입(D23)
- [ ] 대화 원문 범위를 글자 예산으로(D25), 캐시 친화 순서(D28), 롤플레이 마스터 역할 명시(D26)
- [ ] 검색이 헛돈 턴의 깊은 질의 1회 자동 호출(D24)
- [ ] (보류) 오프스크린 캐릭터 진행(D27) — 사용자 테스트 후

### 보류 / 제외
- **보류:** 분기 확장(D4)
- **제외:** 턴별 모델 선택(D1), 파티챗(D15), 엔딩과 실시간 스탯(D13), 이미지 생성(D14)
- **상용 요소 제외:** 크래커, 성인인증, 공유 허브

---

## 4. 구현 순서 근거
- **M0 → M1:** 기억 파이프라인은 "마지막 기록 이후 턴"과 "수정됨 표시된 턴"을 정확히 알아야 합니다. 그래서 메시지 ID·턴 번호 모델(M0)이 먼저입니다. 스토리 격리(D12)도 기억 문서를 스토리에 쓰기 전에 끝나 있어야 합니다.
- **M1 → M2:** 키워드북과 인물 선택(D9)은 같은 매칭 엔진을 씁니다. `/기록`은 M1 파이프라인을 호출합니다.
- **M4:** 상태 패널은 M1의 문서 구조가 정해진 뒤에야 만들 수 있습니다.

## 5. 미결정 사항 (진행하면서 결정)
- 추천 답변, 시작 설정 여러 개를 도입할지
- 기본값 튜닝: 대화 원문을 몇 턴까지 넣을지, 키워드 스캔 범위 N, 문서별 길이 예산, 캐릭터 관리자 동시 실행 수
